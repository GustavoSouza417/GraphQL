REM abaixo, estão todas as dependências
REM utilizadas nestes estudos

npm install -s graphql; REM é o GraphQL em si
npm install -s graphql-tools; REM contém as dependências que realizam o MERGE
npm install -s @graphql-tools/load-files; REM serve para fazer o load dos arquivos do MERGE
npm install -s @graphql-tools/utils; REM contém a interface "IResolvers"
npm install -s apollo-server; REM é o servidor que roda a GraphQL API
npm install -s typescript;
npm install -s ts-node;
npm install --save-dev typescript @types/node;
npm install -s fs;
npm install -s crypto;